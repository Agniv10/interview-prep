numbers = [3, 1, 4, 2, 5, 6, 7, 8, 9]

max_val = -float('inf')
second_max_val = -float('inf')

for num in numbers:
    if num > max_val:
        second_max_val = max_val
        max_val = num
    elif num > second_max_val:
        second_max_val = num

print("2nd largest number:", second_max_val)
